[build-system]
requires = ["setuptools>=77", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "quartz"
readme = "README.md"
authors = []
license = "AGPL-3.0-or-later"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Framework :: Django",
    "Framework :: Django :: 5.2",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Intended Audience :: Information Technology",
]
requires-python = ">=3.11"
dependencies = [
    "arches>=8.0.0,<8.2.0",
    "grafana-django-saml2-auth>=3.12.0",
    "arches_controlled_lists",
    "arches_her @ git+https://github.com/archesproject/arches-her.git@dev/2.0.x",
    "arches_model_viewer @ git+https://github.com/flaxandteal-arches/arches-model-viewer.git",
    "django-storages[azure]",
    "arches_inclusion_rule  @ git+https://github.com/flaxandteal/arches-rbac-permissions.git#subdirectory=arches_inclusion_rule",
    "arches_saved_search    @ git+https://github.com/flaxandteal/arches-rbac-permissions.git#subdirectory=arches_saved_search",
    "arches_user_datatype   @ git+https://github.com/flaxandteal/arches-rbac-permissions.git#subdirectory=arches_user_datatype",
    "arches_semantic_roles  @ git+https://github.com/flaxandteal/arches-rbac-permissions.git#subdirectory=arches_semantic_roles",
    "arches_rbac_permissions @ git+https://github.com/flaxandteal/arches-rbac-permissions.git#subdirectory=arches_rbac_permissions",
]
version = "0.0.1"

[dependency-groups]
dev = [
    "black==24.4.2",
    "coverage",
    "django-silk==5.1.0",
    "livereload",
    "pre-commit==3.8.0",
    "sst",
]

[tool.setuptools]
packages = ["quartz"]
